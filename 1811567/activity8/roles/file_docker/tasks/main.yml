---
- name: docker service
  service:
    name: docker
    state: started
    enabled: yes

- name: web server installation
  docker_image:
    name: romeoz/docker-apache-php
    state: present
    source: pull

- name: db server installation
  docker_image:
    name: mysql/mysql-server:latest
    state: present
    source: pull
# tasks file for roles/file_docker
