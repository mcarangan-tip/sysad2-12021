---
- name: git installation on centos
  yum:
   name: git
   state: latest

- name: cloning prelim exam
  git:
    repo: '{{ prelim_exam }}'
    dest: ./prelim-exam
    version: prelim-exam
# tasks file for roles/centos/git_clone