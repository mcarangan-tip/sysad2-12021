---
- name: git on debian installation
  apt:
   name: git
   state: latest

- name: Clone Prelim Exam
  git:
    repo: '{{ prelim_exam }}'
    dest: ./prelim-exam
    version: prelim-exam
# tasks file for roles/debian/dgit_clone